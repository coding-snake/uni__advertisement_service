{% include 'base/_form.html.twig' with {
    page_title: 'title.ad_edit'|trans({'%id%': ad.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'ad_index',
} %}