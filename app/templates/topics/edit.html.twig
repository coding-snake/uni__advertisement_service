{% include 'base/_form.html.twig' with {
    page_title: 'title.topic_edit'|trans({'%id%': topic.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'topic_index',
} %}