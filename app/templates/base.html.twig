<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{{'title.welcome'}}{% endblock %}</title>
        
        {{ include('base/_styles.html.twig') }}

    </head>
    <body>
        <main role="main" class="container-fluid">
            
            {{ include('base/_navbar.html.twig') }}
            <div class="container-fluid">
                {% include 'base/_flash_messages.html.twig' %}
                {% block body %}{% endblock %}
            </div>
        
        </main>
        {{ include('base/_script.html.twig') }}
    </body>
</html>
