{% extends 'base.html.twig' %}

{% block title %}
	{{ 'title.tag_list'|trans }}
{% endblock %}

{% block body %}
	<h1>{{ 'title.tag_list'|trans }}</h1>
	{% if is_granted('TAG_CREATE') %}
	<button type="button" class="btn btn-secondary" onclick="window.location.href='{{ path('tag_create') }}'" style="margin-top:2rem; margin-bottom:2rem;">
		<img src="{{ asset('images/plus-circle.svg') }}" alt="View" width="30" height="24">
		{{ 'action.create'|trans }}
	</button>
	{% endif %}
	{% if pagination is defined and pagination.items|length %}

		<div class="navigation text-center">
			{{ knp_pagination_render(pagination) }}
		</div>

		<table class="table table-striped">
			<thead>
				<tr>
					<th>{{ knp_pagination_sortable(pagination, 'label.id'|trans, 'tag.id') }}</th>
					<th>{{ knp_pagination_sortable(pagination, 'label.createdAt'|trans, 'tag.createdAt') }}</th>
					<th>{{ knp_pagination_sortable(pagination, 'label.updatedAt'|trans, 'tag.updatedAt') }}</th>
					<th>{{ knp_pagination_sortable(pagination, 'label.name'|trans, 'tag.name') }}</th>
					<th>{{ 'label.actions'|trans }}</th>
				</tr>
			</thead>
			<tbody>
				{% for tag in pagination.items %}
					<tr>
						<td>{{ tag.id }}</td>
						<td>{{ tag.createdAt|format_date('long') }}</td>
						<td>{{ tag.updatedAt|format_date('long') }}</td>
						<td>
							<a href="{{ path('ads_per_tag', { id: tag.id }) }}">
								{{ tag.name }}
							</a>
						</td>
						<td>
							{% if is_granted('TAG_VIEW', tag) %}
								<button type="button" class="btn btn-primary" onclick="window.location.href='{{ path('tag_read', { id: tag.id }) }}'">
									<img src="{{ asset('images/eye.svg') }}" alt="View" width="30" height="24">
									{{ 'action.read'|trans }}
								</button>
							{% endif %}
							{% if is_granted('TAG_EDIT', tag) %}
								<button type="button" class="btn btn-success" onclick="window.location.href='{{ path('tag_edit', { id: tag.id }) }}'">
									<img src="{{ asset('images/pencil-fill.svg') }}" alt="View" width="30" height="24">
									{{ 'action.edit'|trans }}
								</button>
							{% endif %}
							{% if is_granted('TAG_DELETE', tag) %}
								<button type="button" class="btn btn-danger" onclick="window.location.href='{{ path('tag_delete', { id: tag.id }) }}'">
									<img src="{{ asset('images/trash3.svg') }}" alt="View" width="30" height="24">
									{{ 'action.delete'|trans }}
								</button>
							{% endif %}
						</td>
					</tr>
				{% endfor %}
			</tbody>
		</table>

		<div class="navigation text-center">
			{{ knp_pagination_render(pagination) }}
		</div>

	{% else %}
		<p>
			{{ 'message.empty_list'|trans }}
		</p>
	{% endif %}
{% endblock %}
